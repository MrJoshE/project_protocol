name: server
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  logging:
  uuid: ^3.0.6

  config:
    path: "../config"
#   path: ^1.8.0

dev_dependencies:
  pedantic: ^1.10.0
