// import 'dart:convert';

class EnetResponse {
  EnetResponse();

  factory EnetResponse.fromRawResponse(List<int> data) {
    // final rawResponse = utf8.decode(data);

    return EnetResponse();
  }
}
