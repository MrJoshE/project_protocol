class EnetConfig {
  /// The default port that the server will listen on.
  static const int DEFAULT_PORT = 1820;

  /// The default host address of the server that will be listening.
  static const String DEFAULT_HOST = '127.0.0.1';

  /// The default number of milliseconds that the client will wait for a response
  /// from the server before timing out.
  static const int DEFAULT_TIMEOUT = 3000;
}
